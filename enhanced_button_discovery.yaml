# Enhanced Button Discovery for Shrooly - Focus on Likely Pins
esphome:
  name: enhanced-openshrooly
  friendly_name: Enhanced Button Discovery
  project:
    name: "enhanced.openshrooly"
    version: "2.2.0-enhanced-buttons"

esp32:
  board: esp32-s3-devkitc-1
  framework:
    type: arduino
    version: recommended

logger:
  level: DEBUG

api:

ota:
  - platform: esphome

wifi:
  ap:
    ssid: "Enhanced-OpenShrooly-Setup"
    password: "shrooly123"

captive_portal:
web_server:
  port: 80

# Working hardware configuration from OpenShrooly
i2c:
  sda: GPIO36
  scl: GPIO35
  scan: true
  id: bus_a

spi:
  clk_pin: GPIO7
  mosi_pin: GPIO6

font:
  - file: fonts/arial.ttf
    id: font_medium
    size: 12

display:
  - platform: waveshare_epaper
    id: my_display
    cs_pin: GPIO8
    dc_pin: GPIO48
    busy_pin: GPIO38
    reset_pin: GPIO21
    rotation: 90°
    model: 2.90inv2-r2
    update_interval: 15s
    lambda: |-
      it.print(10, 10, id(font_medium), "ENHANCED DISCOVERY");
      it.print(10, 30, id(font_medium), "Testing likely pins");
      it.print(10, 50, id(font_medium), "Press each button");
      it.print(10, 70, id(font_medium), "on the device!");

binary_sensor:
  # Known working button (reference)
  - platform: gpio
    pin:
      number: GPIO5
      mode: INPUT_PULLUP
      inverted: true
    name: "GPIO5 Reset (Working)"
    id: gpio5_button
    on_press:
      - logger.log: 
          format: "🔴 GPIO5 RESET BUTTON - CONFIRMED WORKING!"
          level: ERROR
      - component.update: my_display

  # Most likely candidates for additional buttons on ESP32-S3
  # Test both inverted and non-inverted for each pin
  
  # GPIO0 - Boot button on many ESP32 boards
  - platform: gpio
    pin:
      number: GPIO0
      mode: INPUT_PULLUP
      inverted: true
    name: "GPIO0 Test INV"
    id: gpio0_inv
    on_press:
      - logger.log: 
          format: "🟢 GPIO0 INVERTED PULLUP - FOUND BUTTON!"
          level: ERROR

  - platform: gpio
    pin:
      number: GPIO0
      mode: INPUT_PULLUP
      inverted: false  
    name: "GPIO0 Test NORM"
    id: gpio0_norm
    on_press:
      - logger.log: 
          format: "🟢 GPIO0 NORMAL PULLUP - FOUND BUTTON!"
          level: ERROR

  # GPIO1-4 - Common for user buttons
  - platform: gpio
    pin:
      number: GPIO1
      mode: INPUT_PULLUP
      inverted: true
    name: "GPIO1 Test INV"
    id: gpio1_inv
    on_press:
      - logger.log: 
          format: "🟢 GPIO1 INVERTED PULLUP - FOUND BUTTON!"
          level: ERROR

  - platform: gpio
    pin:
      number: GPIO1
      mode: INPUT_PULLUP
      inverted: false
    name: "GPIO1 Test NORM"  
    id: gpio1_norm
    on_press:
      - logger.log: 
          format: "🟢 GPIO1 NORMAL PULLUP - FOUND BUTTON!"
          level: ERROR

  - platform: gpio
    pin:
      number: GPIO2
      mode: INPUT_PULLUP
      inverted: true
    name: "GPIO2 Test INV"
    id: gpio2_inv
    on_press:
      - logger.log: 
          format: "🟢 GPIO2 INVERTED PULLUP - FOUND BUTTON!"
          level: ERROR

  - platform: gpio
    pin:
      number: GPIO2
      mode: INPUT_PULLUP
      inverted: false
    name: "GPIO2 Test NORM"
    id: gpio2_norm
    on_press:
      - logger.log: 
          format: "🟢 GPIO2 NORMAL PULLUP - FOUND BUTTON!"
          level: ERROR

  - platform: gpio
    pin:
      number: GPIO3
      mode: INPUT_PULLUP
      inverted: true
    name: "GPIO3 Test INV"
    id: gpio3_inv
    on_press:
      - logger.log: 
          format: "🟢 GPIO3 INVERTED PULLUP - FOUND BUTTON!"
          level: ERROR

  - platform: gpio
    pin:
      number: GPIO3
      mode: INPUT_PULLUP
      inverted: false
    name: "GPIO3 Test NORM"
    id: gpio3_norm
    on_press:
      - logger.log: 
          format: "🟢 GPIO3 NORMAL PULLUP - FOUND BUTTON!"
          level: ERROR

  - platform: gpio
    pin:
      number: GPIO4
      mode: INPUT_PULLUP
      inverted: true
    name: "GPIO4 Test INV"
    id: gpio4_inv
    on_press:
      - logger.log: 
          format: "🟢 GPIO4 INVERTED PULLUP - FOUND BUTTON!"
          level: ERROR

  - platform: gpio
    pin:
      number: GPIO4
      mode: INPUT_PULLUP
      inverted: false
    name: "GPIO4 Test NORM"
    id: gpio4_norm
    on_press:
      - logger.log: 
          format: "🟢 GPIO4 NORMAL PULLUP - FOUND BUTTON!"
          level: ERROR

  # GPIO9-18 range (avoiding pins already used)
  - platform: gpio
    pin:
      number: GPIO9
      mode: INPUT_PULLUP
      inverted: true
    name: "GPIO9 Test INV"
    id: gpio9_inv
    on_press:
      - logger.log: 
          format: "🟢 GPIO9 INVERTED PULLUP - FOUND BUTTON!"
          level: ERROR

  - platform: gpio
    pin:
      number: GPIO10
      mode: INPUT_PULLUP
      inverted: true
    name: "GPIO10 Test INV"
    id: gpio10_inv
    on_press:
      - logger.log: 
          format: "🟢 GPIO10 INVERTED PULLUP - FOUND BUTTON!"
          level: ERROR

  - platform: gpio
    pin:
      number: GPIO11
      mode: INPUT_PULLUP
      inverted: true
    name: "GPIO11 Test INV"
    id: gpio11_inv
    on_press:
      - logger.log: 
          format: "🟢 GPIO11 INVERTED PULLUP - FOUND BUTTON!"
          level: ERROR

  - platform: gpio
    pin:
      number: GPIO12
      mode: INPUT_PULLUP
      inverted: true
    name: "GPIO12 Test INV"
    id: gpio12_inv
    on_press:
      - logger.log: 
          format: "🟢 GPIO12 INVERTED PULLUP - FOUND BUTTON!"
          level: ERROR

  - platform: gpio
    pin:
      number: GPIO13
      mode: INPUT_PULLUP
      inverted: true
    name: "GPIO13 Test INV"
    id: gpio13_inv
    on_press:
      - logger.log: 
          format: "🟢 GPIO13 INVERTED PULLUP - FOUND BUTTON!"
          level: ERROR

  # Skip GPIO14 (fan tachometer in original), GPIO15-18

  # Try some higher GPIO numbers  
  - platform: gpio
    pin:
      number: GPIO40
      mode: INPUT_PULLUP
      inverted: true
    name: "GPIO40 Test INV"
    id: gpio40_inv
    on_press:
      - logger.log: 
          format: "🟢 GPIO40 INVERTED PULLUP - FOUND BUTTON!"
          level: ERROR

  - platform: gpio
    pin:
      number: GPIO41
      mode: INPUT_PULLUP
      inverted: true
    name: "GPIO41 Test INV"
    id: gpio41_inv
    on_press:
      - logger.log: 
          format: "🟢 GPIO41 INVERTED PULLUP - FOUND BUTTON!"
          level: ERROR

  - platform: gpio
    pin:
      number: GPIO43
      mode: INPUT_PULLUP
      inverted: true
    name: "GPIO43 Test INV"
    id: gpio43_inv
    on_press:
      - logger.log: 
          format: "🟢 GPIO43 INVERTED PULLUP - FOUND BUTTON!"
          level: ERROR

  - platform: gpio
    pin:
      number: GPIO44
      mode: INPUT_PULLUP
      inverted: true
    name: "GPIO44 Test INV"
    id: gpio44_inv
    on_press:
      - logger.log: 
          format: "🟢 GPIO44 INVERTED PULLUP - FOUND BUTTON!"
          level: ERROR

sensor:
  - platform: sht4x
    i2c_id: bus_a
    temperature:
      name: "Temperature"
      id: temperature
    humidity:
      name: "Humidity"  
      id: humidity
    update_interval: 10s

interval:
  - interval: 8s
    then:
      - logger.log: 
          format: "🔍 ENHANCED DISCOVERY - Press each button on the device"
          level: ERROR
      - logger.log:
          format: "📊 %.1f°C, %.1f%% - Testing likely GPIO pins for buttons"
          args: [ 'id(temperature).state', 'id(humidity).state' ]
          level: INFO